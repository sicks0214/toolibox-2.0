# 📘《插件接入主站的唯一正确流程规范 v1.0》

**适用范围**
本指示文档用于规范：
**工具在本地独立开发后，如何正确接入并部署到单体主站系统中**

---

## 1. 架构前提（强制前提，不可违反）

### 1.1 系统形态（必须认知一致）

本项目是：

* ✅ 单体前端（Next.js，端口 3000）
* ✅ 单体后端（Node.js / Express，端口 8000）
* ✅ 插件系统仅作为 **后端代码组织方式**

**生产环境中：**

* ❌ 不存在插件独立程序
* ❌ 不存在插件独立端口
* ❌ 不存在插件独立部署
* ❌ 不存在第二个工具服务

---

### 1.2 插件的真实定义

> **插件 = 最终被合并进主后端进程的代码模块**

插件可以：

* 在本地独立开发
* 在本地独立测试

插件**不能**：

* 在生产环境独立运行
* 在 VPS 上单独启动
* 被 Nginx 指向为独立 upstream

---

## 2. 插件开发阶段（本地）

### 2.1 本地插件开发结构（推荐）

```txt
local-dev/pdf-merge/
├── plugin.json
├── ui.json
├── schema.json
└── handler/
    └── index.ts
```

说明：

* 该目录**不属于主站**
* 可用于：

  * 单元测试
  * mock handler
  * 本地调试

⚠️ 此阶段的"独立"仅限开发语义，不具备部署意义。

---

### 2.2 本地开发允许行为

✅ 启动 mock server
✅ 单独调试 handler
✅ 使用临时端口
✅ 本地写测试脚本

这些行为**不会改变插件的最终部署方式**。

---

## 3. 插件接入主站（核心步骤）

### 3.1 接入动作（唯一允许的方式）

> **插件接入 = 代码合并**

执行以下操作：

```txt
复制插件目录 →
backend/plugins/{plugin-name}/
```

示例：

```txt
backend/plugins/pdf-merge/
├── plugin.json
├── ui.json
├── schema.json
└── handler/index.ts
```

---

### 3.2 接入后允许的操作

* 不修改前端代码
* 不修改路由
* 不新增端口
* 不新增服务
* 不新增 Nginx 配置

**唯一需要做的事：**

```bash
重启后端服务
```

---

## 4. 插件接入后的系统行为（正确预期）

插件被接入后，系统会自动完成：

1. 后端启动时扫描 `backend/plugins/*`
2. 加载 plugin.json / ui.json / schema.json
3. 自动注册 API 路由：

   ```
   POST /api/{category}/{slug}
   ```
4. `/api/plugins` 返回该工具元数据
5. 前端自动生成：

   * 分类页工具卡片
   * 工具页面 URL
   * SEO 内容
   * SchemaRenderer UI

---

## 5. 部署阶段（VPS / 生产环境）

### 5.1 VPS 上允许存在的进程（固定）

```txt
- Next.js 前端（3000）
- Node.js 后端（8000）
```

示意：

```txt
Nginx
 ├─ /        → 3000 (Next.js)
 └─ /api/*   → 8000 (Backend)
```

---

### 5.2 插件在 VPS 上的真实形态

```txt
backend/plugins/*
```

插件：

* ❌ 不启动
* ❌ 不监听端口
* ❌ 不作为进程存在
* ✅ 被主后端进程加载

---

## 6. 明确禁止的错误做法（重点）

以下行为 **一律视为错误部署**：

❌ 为插件新建 Node 服务
❌ 为插件分配新端口
❌ 为插件写独立 Docker 容器
❌ 为插件新增 Nginx upstream
❌ 把插件当成"第二个工具系统"
❌ VPS 上出现第三个 Node 进程

---

## 7. AI 指导部署时的校验规则（防带偏）

当任何 AI 给出部署建议时，只做一件事：

### 🔍 快速校验清单

如果 AI 建议中出现以下任意一项：

* "再部署一个服务"
* "插件单独运行"
* "为工具起一个新程序"
* "工具系统单独部署"
* "新增端口"

👉 **立即判定为不适用于本项目，直接忽略。**

---

## 8. 插件接入部署自检清单（上线前必查）

上线前，请逐条确认：

* [ ] VPS 上只有 2 个 Node 进程
* [ ] Nginx 只有一个 server block
* [ ] 插件只存在于 `backend/plugins`
* [ ] 插件未监听任何端口
* [ ] 插件仅通过主后端 API 被调用
* [ ] 前端未为插件写任何专用代码

**全部满足 → 接入正确**

---

## 9. 核心原则

> * 插件是"开发时可独立，运行时必须合并"的代码
> * 单体是部署事实，不是妥协方案
> * 插件系统解决扩展问题，不解决部署问题
> * 任何破坏单体部署的建议，都是错误建议

---

## 10. 本文档的地位（重要）

> 本文档是 **插件接入与部署的最高优先级规范**
> 当其他文档、AI 建议、个人想法与本文档冲突时：
> **以本文档为准**

---
