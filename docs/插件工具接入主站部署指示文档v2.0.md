# 📘 插件工具接入主站部署指示文档 v2.0（Markdown 版）

## 目录

1. 架构前提（强制，不可违反）
2. 插件开发阶段（本地）
3. 插件接入主站（唯一正确方法）
4. 插件接入后的系统行为
5. 部署阶段（生产/VPS）
6. 明确禁止的错误做法
7. AI 建议校验规则
8. 插件部署自检清单
9. 核心原则
10. 插件 UI 规范（ui.json / schema.json / plugin.json）
11. 文档地位

---

## 1. 架构前提（强制，不可违反）

本项目为：

* 单体前端（Next.js，3000）
* 单体后端（Node.js / Express，8000）
* 插件 = 后端代码模块

生产环境：

* 不存在插件独立运行
* 不存在插件独立端口
* 不存在插件独立部署
* 不存在第二个工具服务

插件最终必须运行在主后端进程中。

---

## 2. 插件开发阶段（本地）

本地推荐结构：

```
local-dev/{plugin}/
├── plugin.json
├── schema.json
├── ui.json
└── handler/
    └── index.ts
```

本地允许：

* mock server
* 单独调试 handler
* 临时端口
* 单元测试

但这些不会影响最终部署方式。

---

## 3. 插件接入主站（唯一方式）

接入方式：

```
复制插件目录 → backend/plugins/{plugin}/
```

结构：

```
backend/plugins/{plugin}/
├── plugin.json
├── schema.json
├── ui.json
└── handler/index.ts
```

接入后无需：

* 修改前端
* 修改路由
* 新增端口
* 新建服务

唯一需要的步骤：

```
重启后端服务
```

---

## 4. 插件接入后的系统行为

插件接入后系统自动完成：

1. 扫描 `backend/plugins/*`
2. 加载 JSON 元数据
3. 注册 API：

   ```
   POST /api/{category}/{slug}
   ```
4. `/api/plugins` 自动包含工具信息
5. 前端自动生成：

   * 工具卡片
   * 工具页 URL
   * SEO
   * UI（依赖 ui.json + schema.json）

若 JSON 不合法 → 自动降级为简易 fallback UI。

---

## 5. 部署阶段（生产/VPS）

允许的进程：

```
Next.js 前端（3000）
Node.js 后端（8000）
```

Nginx 结构：

```
/        → 3000
/api/*   → 8000
```

插件在生产环境中：

```
backend/plugins/*
```

插件不会启动独立服务或端口。

---

## 6. 明确禁止的错误做法

以下均视为错误部署：

* 插件开新 Node 服务
* 插件监听端口
* 插件独立部署
* 插件写独立 Docker
* 插件添加独立 Nginx upstream
* 将插件理解为第二套工具系统

---

## 7. AI 建议校验规则

如果 AI 建议出现：

* 新服务
* 新端口
* 插件单独运行
* 工具独立部署
* 第二个系统

全部判定为不适用于本项目。

---

## 8. 插件部署自检清单

上线前确认：

* [ ] VPS 上只有 2 个 Node 进程
* [ ] Nginx 只有 1 个 server block
* [ ] 插件仅位于 `/backend/plugins/`
* [ ] 插件不监听任何端口
* [ ] 插件仅通过主后端 API 调用
* [ ] 前端未为插件编写专用代码
* [ ] ui.json / schema.json 格式正确

全部满足则接入正确。

---

## 9. 核心原则

* 插件开发可独立，运行必须合并
* 单体部署是事实架构
* 插件系统负责扩展，不负责部署
* 任何破坏单体架构的建议均为错误

---

## 10. 插件 UI & Schema 规范（完全重写版 · 与主站代码保持一致）

**适用位置：**
- `backend/plugins/{plugin}/plugin.json`
- `backend/plugins/{plugin}/schema.json`
- `backend/plugins/{plugin}/ui.json`

**对应代码位置：**
- `backend/src/plugin-loader.ts:4-9` — PluginConfig
- `backend/src/plugin-loader.ts:11-18` — PluginUI
- `backend/src/plugin-loader.ts:20-35` — PluginSchema

---

### 10.1 PluginConfig（plugin.json）

主站会读取 `plugin.json` 来确定插件的分类、标识符、API 路径等。

```json
{
  "slug": "pdf-split",
  "category": "pdf",
  "apiPath": "/api/pdf/pdf-split",
  "order": 1
}
```

**字段说明：**

| 字段 | 类型 | 必填 | 作用 |
|------|------|------|------|
| slug | string | 是 | 工具唯一标识 |
| category | string | 是 | 工具分类目录 |
| apiPath | string | 是 | API 完整路径 |
| order | number | 是 | 同分类下的排序顺序 |

---

### 10.2 PluginUI（ui.json）

前端 UI 完全依赖 `ui.json` 生成页面内容，包括标题、描述、图标、FAQ 等。

**📌 ui.json 真实结构（来自 plugin-loader.ts）**

```typescript
interface PluginUI {
  title: Record<string, string>;
  description: Record<string, string>;
  h1: Record<string, string>;
  icon: string;
  faq: Record<string, Array<{ question: string; answer: string }>>>;
  categoryName?: Record<string, string>;
  // 扩展字段（可选）
  subtitle?: Record<string, string>;
  features?: Record<string, string[]>;
  breadcrumb?: Record<string, string>;
  upload?: Record<string, string>;
  actions?: Record<string, string[]>;
  feedback?: Record<string, string>;
  useCases?: Record<string, string[]>;
  howTo?: Record<string, string[]>;
}
```

**📄 ui.json 基础模板（必填字段）**

```json
{
  "title": {
    "en": "Split PDF Online",
    "zh": "PDF 分割工具"
  },
  "description": {
    "en": "Split PDF pages into multiple files online.",
    "zh": "将 PDF 页面在线分割成多个文件。"
  },
  "h1": {
    "en": "Split Your PDF",
    "zh": "拆分 PDF 文件"
  },
  "icon": "pdf-split",
  "faq": {
    "en": [
      {
        "question": "Is this tool free?",
        "answer": "Yes, it's completely free."
      }
    ],
    "zh": [
      {
        "question": "这个工具免费吗？",
        "answer": "是的,完全免费。"
      }
    ]
  },
  "categoryName": {
    "en": "PDF Tools",
    "zh": "PDF 工具"
  }
}
```

**📄 ui.json 完整模板（包含扩展字段）**

```json
{
  "title": {
    "en": "Split PDF Online",
    "zh": "PDF 分割工具"
  },
  "description": {
    "en": "Split PDF pages into multiple files online.",
    "zh": "将 PDF 页面在线分割成多个文件。"
  },
  "subtitle": {
    "en": "Fast, secure, and easy to use",
    "zh": "快速、安全、易用"
  },
  "h1": {
    "en": "Split Your PDF",
    "zh": "拆分 PDF 文件"
  },
  "icon": "pdf-split",
  "features": {
    "en": [
      "No file size limit",
      "Batch processing supported",
      "100% secure and private"
    ],
    "zh": [
      "无文件大小限制",
      "支持批量处理",
      "100% 安全私密"
    ]
  },
  "upload": {
    "en": "Please upload a PDF file to split",
    "zh": "请上传需要分割的 PDF 文件"
  },
  "actions": {
    "en": [
      "Upload your PDF file",
      "Select page range to split",
      "Click split button to process"
    ],
    "zh": [
      "上传您的 PDF 文件",
      "选择要分割的页面范围",
      "点击分割按钮进行处理"
    ]
  },
  "feedback": {
    "en": "Your file will be automatically deleted after processing",
    "zh": "处理完成后文件将自动删除"
  },
  "useCases": {
    "en": [
      "Extract specific pages from a large PDF",
      "Split PDF for easier sharing",
      "Separate chapters or sections"
    ],
    "zh": [
      "从大型 PDF 中提取特定页面",
      "分割 PDF 以便于分享",
      "分离章节或部分"
    ]
  },
  "howTo": {
    "en": [
      "Click or drag to upload your PDF file",
      "Enter the page range you want to split (e.g., 1-5, 10-15)",
      "Click the 'Split PDF' button",
      "Download the split PDF files"
    ],
    "zh": [
      "点击或拖拽上传您的 PDF 文件",
      "输入要分割的页面范围（例如：1-5, 10-15）",
      "点击"分割 PDF"按钮",
      "下载分割后的 PDF 文件"
    ]
  },
  "faq": {
    "en": [
      {
        "question": "Is this tool free?",
        "answer": "Yes, it's completely free."
      }
    ],
    "zh": [
      {
        "question": "这个工具免费吗？",
        "answer": "是的，完全免费。"
      }
    ]
  },
  "categoryName": {
    "en": "PDF Tools",
    "zh": "PDF 工具"
  }
}
```

**扩展字段说明：**

| 字段 | 类型 | 必填 | 显示位置 | 作用 |
|------|------|------|---------|------|
| subtitle | Record<string, string> | 否 | 标题下方 | 副标题，补充说明 |
| features | Record<string, string[]> | 否 | 工具操作区上方 | 功能特性列表 |
| upload | Record<string, string> | 否 | 工具操作区上方 | 上传说明提示 |
| actions | Record<string, string[]> | 否 | 工具操作区下方 | 操作步骤说明 |
| feedback | Record<string, string> | 否 | FAQ 上方 | 反馈信息提示 |
| useCases | Record<string, string[]> | 否 | FAQ 下方 | 使用场景列表 |
| howTo | Record<string, string[]> | 否 | 最下方 | 详细使用方法 |

---

### 10.3 PluginSchema（schema.json）

`schema` 负责描述表单界面结构：上传文件、选项字段、提交按钮等。

**📌 schema.json 真实结构（来自 plugin-loader.ts）**

```typescript
interface PluginSchema {
  upload: {
    multiple: boolean;
    maxFiles: number;
    types: string[];
    maxSize: number;
  };
  options: Array<{
    name: string;
    type: string;
    label: Record<string, string>;
    [key: string]: any;
  }>;
  submitText: Record<string, string>;
  outputType: string;
}
```

**📄 schema.json 模板（完全可用）**

```json
{
  "upload": {
    "multiple": false,
    "maxFiles": 1,
    "types": ["pdf"],
    "maxSize": 50
  },
  "options": [
    {
      "name": "range",
      "type": "text",
      "label": {
        "en": "Page Range",
        "zh": "拆分页面范围"
      }
    }
  ],
  "submitText": {
    "en": "Split PDF",
    "zh": "拆分 PDF"
  },
  "outputType": "file"
}
```

---

### 10.4 前端如何渲染 PluginSchema 和扩展字段

主站前端执行以下行为：

**Schema 字段渲染（SchemaRenderer 组件）：**

| schema 字段 | 前端表现 |
|------------|---------|
| upload | 渲染上传文件区域（单/多文件、数量限制、文件类型、大小限制） |
| options | 渲染输入框、下拉框、开关等 UI 组件 |
| submitText | 显示提交按钮文本 |
| outputType | 用于渲染输出结果类型（file / image / text） |

**UI 扩展字段渲染（工具详情页）：**

| ui.json 字段 | 渲染位置 | 样式 | 说明 |
|-------------|---------|------|------|
| subtitle | 标题下方 | 灰色小字 | 副标题补充说明 |
| features | 工具操作区上方 | 蓝色背景卡片 + ✓ 图标 | 功能特性列表 |
| upload | 工具操作区上方 | 黄色左边框提示框 | 上传说明 |
| actions | 工具操作区下方 | 绿色背景卡片 + 有序列表 | 操作步骤 |
| feedback | FAQ 上方 | 紫色左边框提示框 | 反馈信息 |
| useCases | FAQ 下方 | 靛蓝色背景卡片 + 无序列表 | 使用场景 |
| howTo | 最下方 | 青色背景卡片 + 有序列表 | 详细使用方法 |

**渲染顺序（从上到下）：**
1. 面包屑导航
2. 工具图标 + 标题（h1）
3. 描述（description）
4. 副标题（subtitle）- 可选
5. 功能特性（features）- 可选
6. 上传说明（upload）- 可选
7. **工具操作区（SchemaRenderer）**
8. 操作步骤（actions）- 可选
9. 反馈信息（feedback）- 可选
10. 常见问题（faq）
11. 使用场景（useCases）- 可选
12. 使用方法（howTo）- 可选

**代码位置：**
- Schema 渲染：`frontend/main/src/components/tool/SchemaRenderer.tsx`
- 扩展字段渲染：`frontend/main/src/app/[locale]/[categoryId]/[slug]/page.tsx`

---

### 10.5 插件文件结构（最终标准）

```
backend/plugins/{your-plugin}/
  ├── handler/
  │   └── index.ts     # 必须：执行主逻辑
  ├── plugin.json      # 必须：分类、slug、API路径、排序
  ├── ui.json          # 必须：标题、描述、FAQ、图标
  ├── schema.json      # 可选：UI 表单定义
```

---

### 10.6 一个完整示例插件

```
backend/plugins/pdf-split/
  ├── handler/
  │   └── index.ts
  ├── schema.json
  ├── ui.json
  ├── plugin.json
```

---

## 11. 文档地位

当其他文档、AI 建议或个人想法与本规范冲突时：

**以本指示文档 v2.0 为准。**

---

