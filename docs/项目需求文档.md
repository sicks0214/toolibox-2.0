# TooliBox.com 项目需求文档 v2.0

**版本**：v2.0（首页+占位版）
**日期**：2025-12-08
**状态**：待确认

---

## 一、项目概述

### 1.1 项目定位
- **网站名称**：TooliBox
- **域名**：toolibox.com
- **定位**：免费在线工具聚合平台（展示型网站）
- **目标用户**：全球用户（开发者、设计师、内容创作者、普通用户）

### 1.2 **本期开发范围**（重要）

#### ✅ 本期实现
1. **首页**：展示5个类目的渐变卡片，每个卡片显示类目名称 + 工具数量
2. **类目页**：显示该类目下6个工具的占位卡片（工具名称 + 图标 + 简短描述）
3. **工具占位页**：统一的"Coming Soon"模板页面，显示工具名称和即将上线提示
4. **说明页面**：About、Privacy Policy、Terms of Service
5. **反馈系统**：完整的反馈表单 + 后端API + PostgreSQL存储 + Cloudflare R2备份
6. **国际化**：中英文双语支持

#### ✅ 技术设施
- ✅ PostgreSQL 数据库（存储反馈数据）
- ✅ Cloudflare R2（反馈数据定期备份）
- ✅ Node.js 后端 API（处理反馈提交）

#### ❌ 本期不做
- ❌ 任何工具的实际功能（30个工具都是占位）
- ❌ 用户登录/注册系统
- ❌ 搜索功能

---

## 二、国际化方案

### 2.1 语言支持
- **默认语言**：英文 (en)
- **支持语言**：中文 (zh)

### 2.2 URL 路由结构
```
英文（默认）
├── /                                    # 首页
├── /category/text-tools                 # 类目页
├── /tools/word-counter                  # 工具占位页
├── /about                               # 关于我们
├── /privacy                             # 隐私政策
├── /terms                               # 服务条款
└── /feedback                            # 反馈页面

中文
├── /zh/                                 # 中文首页
├── /zh/category/text-tools              # 类目页
├── /zh/tools/word-counter               # 工具占位页
├── /zh/about                            # 关于我们
├── /zh/privacy                          # 隐私政策
├── /zh/terms                            # 服务条款
└── /zh/feedback                         # 反馈页面
```

### 2.3 语言切换
- **组件形式**：下拉框（Dropdown）形式，而非简单按钮
- **位置**：Header 右侧
- **当前支持语言**：
  - English (EN)
  - 中文 (ZH)
- **扩展性**：下拉框设计便于后期添加更多语言（日语、韩语、西班牙语等）
- **切换逻辑**：切换后保持当前页面路径，仅改变语言前缀
- **示例**：
  ```
  [🌐 English ▼]  ← 点击展开
      ├─ English
      └─ 中文
  ```

---

## 三、5大类目与30个工具占位

> **说明**：以下工具仅为占位数据（名称、slug、简短描述），**不实现具体功能**

### 3.1 📝 Text Tools（文本工具）

| # | 工具名称 (EN)          | 工具名称 (ZH)   | Slug                | 简短描述 (EN)                      |
|---|----------------------|----------------|---------------------|------------------------------------|
| 1 | Word Counter         | 字数统计工具    | word-counter        | Count words, characters, and more  |
| 2 | Case Converter       | 大小写转换器    | case-converter      | Convert text case instantly        |
| 3 | Text Diff Checker    | 文本差异对比    | text-diff           | Compare two texts side by side     |
| 4 | Lorem Ipsum Generator| 占位文本生成器  | lorem-ipsum         | Generate placeholder text          |
| 5 | Remove Line Breaks   | 删除换行符      | remove-line-breaks  | Remove unwanted line breaks        |
| 6 | Reverse Text         | 文本反转工具    | reverse-text        | Reverse your text instantly        |

### 3.2 📁 File Tools（文件工具）

| # | 工具名称 (EN)          | 工具名称 (ZH)   | Slug                | 简短描述 (EN)                      |
|---|----------------------|----------------|---------------------|------------------------------------|
| 1 | PDF Merger           | PDF合并工具     | pdf-merger          | Merge multiple PDFs into one       |
| 2 | PDF to Image         | PDF转图片       | pdf-to-image        | Convert PDF pages to images        |
| 3 | File Compressor      | 文件压缩工具    | file-compressor     | Compress files to save space       |
| 4 | CSV to JSON          | CSV转JSON       | csv-to-json         | Convert CSV files to JSON format   |
| 5 | Excel Viewer         | Excel在线查看器 | excel-viewer        | View Excel files online            |
| 6 | File Hash Calculator | 文件哈希计算器  | file-hash           | Calculate MD5, SHA256 hashes       |

### 3.3 🖼️ Image Tools（图像工具）

| # | 工具名称 (EN)          | 工具名称 (ZH)   | Slug                | 简短描述 (EN)                      |
|---|----------------------|----------------|---------------------|------------------------------------|
| 1 | Image Compressor     | 图片压缩工具    | image-compressor    | Compress images without quality loss|
| 2 | Image Resizer        | 图片尺寸调整    | image-resizer       | Resize images to any dimension     |
| 3 | Image Format Converter| 图片格式转换   | image-converter     | Convert between JPG, PNG, WEBP     |
| 4 | Image Cropper        | 图片裁剪工具    | image-cropper       | Crop images to perfect size        |
| 5 | QR Code Generator    | 二维码生成器    | qr-code-generator   | Generate QR codes instantly        |
| 6 | Color Picker         | 颜色提取器      | color-picker        | Pick colors from images            |

### 3.4 ⚡ Generate Tools（生成工具）

| # | 工具名称 (EN)          | 工具名称 (ZH)   | Slug                | 简短描述 (EN)                      |
|---|----------------------|----------------|---------------------|------------------------------------|
| 1 | Password Generator   | 密码生成器      | password-generator  | Generate strong passwords          |
| 2 | UUID Generator       | UUID生成器      | uuid-generator      | Generate unique identifiers        |
| 3 | Fake Data Generator  | 测试数据生成器  | fake-data-generator | Generate fake data for testing     |
| 4 | Lorem Image Generator| 占位图片生成器  | lorem-image         | Generate placeholder images        |
| 5 | Gradient Generator   | 渐变色生成器    | gradient-generator  | Create beautiful CSS gradients     |
| 6 | Invoice Generator    | 发票生成器      | invoice-generator   | Generate professional invoices     |

### 3.5 💻 Developer Tools（开发者工具）

| # | 工具名称 (EN)          | 工具名称 (ZH)   | Slug                | 简短描述 (EN)                      |
|---|----------------------|----------------|---------------------|------------------------------------|
| 1 | JSON Formatter       | JSON格式化工具  | json-formatter      | Format and validate JSON           |
| 2 | Base64 Encoder       | Base64编码工具  | base64-encoder      | Encode and decode Base64           |
| 3 | URL Encoder          | URL编码工具     | url-encoder         | Encode and decode URLs             |
| 4 | Regex Tester         | 正则表达式测试  | regex-tester        | Test regular expressions           |
| 5 | Code Beautifier      | 代码美化工具    | code-beautifier     | Beautify JavaScript, CSS, HTML     |
| 6 | Unix Timestamp Converter| Unix时间戳转换 | timestamp-converter | Convert Unix timestamps            |

---

## 四、页面设计详情

### 4.1 首页（Homepage）

**URL**: `/` 或 `/zh/`

#### 页面结构
```
┌──────────────────────────────────────────────────────────┐
│  [Logo] TooliBox    [About] [🌐 English ▼] [Feedback]   │ ← Header (固定顶部)
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                     Hero Section                         │
│                                                          │
│              🛠️ Free Online Tools for Everyone           │
│         Make your work easier with 30+ tools            │
│              No sign-up required. 100% free.            │
│                                                          │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                   5 Category Cards                       │
│                  (渐变卡片，响应式布局)                   │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  📝 Text Tools                         6 tools →   │ │ ← 卡片1: 蓝紫渐变
│  │  Process and manipulate text easily                │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  📁 File Tools                         6 tools →   │ │ ← 卡片2: 绿青渐变
│  │  Convert and manage your files                     │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  🖼️ Image Tools                        6 tools →   │ │ ← 卡片3: 粉橙渐变
│  │  Edit and optimize images                          │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  ⚡ Generate Tools                     6 tools →   │ │ ← 卡片4: 蓝青渐变
│  │  Generate passwords, UUIDs, and more               │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  💻 Developer Tools                    6 tools →   │ │ ← 卡片5: 深蓝渐变
│  │  Essential tools for developers                    │ │
│  └────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                        Footer                            │
│                                                          │
│  © 2025 TooliBox. All rights reserved.                  │
│  [About] [Privacy Policy] [Terms of Service] [Feedback] │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

#### 渐变色方案（基于 colors.css）
```css
/* 主题色板（来自 colors.css）*/
--color-primary: #4A9C82;      /* 主色调：清新绿 */
--color-background: #F5F7F2;   /* 背景色：浅灰绿 */
--color-neutral: #2C3E3A;      /* 深色：深绿灰 */
--color-secondary: #A8D5BA;    /* 辅助色：浅绿 */
--color-accent: #FF9F66;       /* 强调色：橙色 */
--color-surface: #E8F0EB;      /* 表面色：淡绿 */

/* 5个类目卡片渐变色（融合主题色） */
Text Tools:      linear-gradient(135deg, #4A9C82 0%, #2C3E3A 100%)  /* 主绿→深绿灰 */
File Tools:      linear-gradient(135deg, #A8D5BA 0%, #4A9C82 100%)  /* 浅绿→主绿 */
Image Tools:     linear-gradient(135deg, #FF9F66 0%, #4A9C82 100%)  /* 橙色→主绿 */
Generate Tools:  linear-gradient(135deg, #4A9C82 0%, #A8D5BA 100%)  /* 主绿→浅绿 */
Developer Tools: linear-gradient(135deg, #2C3E3A 0%, #4A9C82 100%)  /* 深绿灰→主绿 */
```

#### 交互效果
- 卡片悬停：轻微上浮（translateY: -8px）+ 阴影加深
- 点击卡片 → 跳转到对应类目页
- 响应式：桌面端2列，平板1列，手机1列

---

### 4.2 类目页（Category Page）

**URL**: `/category/[slug]` 或 `/zh/category/[slug]`

#### 页面结构
```
┌──────────────────────────────────────────────────────────┐
│  [Logo] TooliBox    [About] [🌐 English ▼] [Feedback]   │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  Home > Text Tools                                       │ ← 面包屑导航
│                                                          │
│  📝 Text Tools                                           │ ← 类目标题
│  6 free text processing tools                           │ ← 类目描述
│                                                          │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│              6 Tool Cards (3列网格布局)                   │
│                                                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ 📊       │  │ 🔤       │  │ ⚖️       │              │
│  │ Word     │  │ Case     │  │ Text     │              │
│  │ Counter  │  │ Converter│  │ Diff     │              │
│  │          │  │          │  │          │              │
│  │ Count... │  │ Convert..│  │ Compare..│              │
│  └──────────┘  └──────────┘  └──────────┘              │
│                                                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ 📝       │  │ 🔄       │  │ ↩️       │              │
│  │ Lorem    │  │ Remove   │  │ Reverse  │              │
│  │ Ipsum    │  │ Breaks   │  │ Text     │              │
│  │          │  │          │  │          │              │
│  │ Generate.│  │ Remove...│  │ Reverse..│              │
│  └──────────┘  └──────────┘  └──────────┘              │
│                                                          │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  [Footer]                                                │
└──────────────────────────────────────────────────────────┘
```

#### 工具卡片设计
- 白色背景，圆角边框
- 悬停时显示淡蓝色边框
- 图标 + 工具名称 + 简短描述（1行）
- 点击进入工具占位页

---

### 4.3 工具占位页（Tool Placeholder Page）

**URL**: `/tools/[slug]` 或 `/zh/tools/[slug]`

#### 页面结构（统一模板）
```
┌──────────────────────────────────────────────────────────┐
│  [Logo] TooliBox    [About] [🌐 English ▼] [Feedback]   │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  Home > Text Tools > Word Counter                        │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                                                          │
│                      📊 Word Counter                     │
│                                                          │
│                    🚧 Coming Soon 🚧                     │
│                                                          │
│        This tool is currently under development.         │
│           We'll notify you when it's ready!              │
│                                                          │
│               [← Back to Text Tools]                     │
│                                                          │
│              [📬 Request Early Access]                   │
│                                                          │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  About this tool:                                        │
│  Count words, characters, and more in your text.         │
│                                                          │
│  Related Tools:                                          │
│  [Case Converter] [Text Diff] [Lorem Ipsum]             │
└──────────────────────────────────────────────────────────┘
```

#### 功能说明
- 所有30个工具页面使用相同的占位模板
- 显示工具名称 + "Coming Soon"提示
- "Request Early Access"按钮链接到反馈页面
- 底部显示相关工具推荐（同类目其他工具）

---

### 4.4 About 页面

**URL**: `/about` 或 `/zh/about`

```
┌──────────────────────────────────────────────────────────┐
│                    About TooliBox                        │
│                                                          │
│  TooliBox is a free online tool platform designed to    │
│  make your daily tasks easier. From text processing to  │
│  image editing, we provide simple and powerful tools.   │
│                                                          │
│  Our Mission:                                            │
│  - Provide 100% free tools                              │
│  - No registration required                             │
│  - Fast and secure                                      │
│  - Privacy-focused                                      │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

### 4.5 Privacy Policy 页面

**URL**: `/privacy` 或 `/zh/privacy`

```
┌──────────────────────────────────────────────────────────┐
│                    Privacy Policy                        │
│                                                          │
│  Last updated: 2025-12-08                               │
│                                                          │
│  1. Information We Collect                              │
│  We do not collect any personal information...          │
│                                                          │
│  2. How We Use Your Data                                │
│  ...                                                    │
│                                                          │
│  3. Third-Party Services                                │
│  ...                                                    │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

### 4.6 Terms of Service 页面

**URL**: `/terms` 或 `/zh/terms`

```
┌──────────────────────────────────────────────────────────┐
│                  Terms of Service                        │
│                                                          │
│  Last updated: 2025-12-08                               │
│                                                          │
│  1. Acceptance of Terms                                 │
│  By using TooliBox, you agree to...                     │
│                                                          │
│  2. Use License                                         │
│  ...                                                    │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

### 4.7 Feedback 页面

**URL**: `/feedback` 或 `/zh/feedback`

```
┌──────────────────────────────────────────────────────────┐
│                  📬 Send us Feedback                     │
│                                                          │
│  Name (Optional)                                        │
│  [_________________________________]                    │
│                                                          │
│  Email *                                                │
│  [_________________________________]                    │
│                                                          │
│  Subject                                                │
│  [_________________________________]                    │
│                                                          │
│  Message *                                              │
│  [_________________________________]                    │
│  [_________________________________]                    │
│  [_________________________________]                    │
│                                                          │
│  [ Submit Feedback ]                                    │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

**反馈提交流程**：
1. 前端表单验证
2. 提交到后端 API：`POST /api/feedback`
3. 后端存储到 PostgreSQL 数据库
4. 定时任务每日备份到 Cloudflare R2
5. 返回成功提示给用户

**数据库表设计**：
```sql
CREATE TABLE feedback (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100),
  email VARCHAR(100) NOT NULL,
  subject VARCHAR(200),
  message TEXT NOT NULL,
  tool_name VARCHAR(100),                    -- 来源工具标识（如 'word-counter'）
  status VARCHAR(50) DEFAULT 'pending',      -- 处理状态：pending/in_progress/resolved/closed
  user_agent TEXT,
  ip_address VARCHAR(45),
  created_at TIMESTAMP DEFAULT NOW(),
  backed_up_to_r2 BOOLEAN DEFAULT FALSE
);

-- 创建索引以提升查询性能
CREATE INDEX idx_feedback_status ON feedback(status);
CREATE INDEX idx_feedback_tool_name ON feedback(tool_name);
CREATE INDEX idx_feedback_created_at ON feedback(created_at DESC);
```

**字段说明**：
- `tool_name`：标识反馈来源工具（从工具页提交时记录工具 slug，从反馈页提交可为空）
- `status`：反馈处理状态
  - `pending`：待处理
  - `in_progress`：处理中
  - `resolved`：已解决
  - `closed`：已关闭

**R2 备份策略**：
- 每日凌晨2点自动备份
- 备份格式：`backup/feedback/YYYY-MM-DD.json`
- 备份后更新 `backed_up_to_r2` 字段

---

## 五、技术架构

### 5.1 技术栈

#### 前端
- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS（基于 colors.css 配色）
- **UI组件**: shadcn/ui（可选，或纯Tailwind）
- **国际化**: next-intl
- **图标**: lucide-react 或 react-icons
- **HTTP客户端**: axios

#### 后端
- **框架**: Node.js + Express
- **语言**: TypeScript / JavaScript
- **数据库**: PostgreSQL
- **ORM**: Prisma
- **对象存储**: Cloudflare R2
- **定时任务**: node-cron（用于数据备份）

#### 数据存储
- **工具列表**: JSON配置文件（`/data/tools.json`）
- **类目数据**: JSON配置文件（`/data/categories.json`）
- **翻译文本**: `/locales/en.json` 和 `/locales/zh.json`
- **反馈数据**: PostgreSQL（主存储）+ Cloudflare R2（定期备份）

#### 部署
- **VPS**: Ubuntu 22.04 + Nginx
- **本地开发**: `npm run dev` 命令运行前后端
- **数据库**: 直接在VPS安装PostgreSQL（**不使用Docker**）
- **SSL**: Let's Encrypt (Certbot)
- **CDN**: Cloudflare（可选）

**VPS数据库安装步骤**：
```bash
# 安装 PostgreSQL
sudo apt update
sudo apt install postgresql postgresql-contrib

# 启动并设置开机自启
sudo systemctl start postgresql
sudo systemctl enable postgresql

# 创建数据库和用户
sudo -u postgres psql
CREATE DATABASE toolibox;
CREATE USER toolibox_user WITH PASSWORD 'your_secure_password';
GRANT ALL PRIVILEGES ON DATABASE toolibox TO toolibox_user;
\q
```

### 5.2 端口配置
```
前端 (Next.js):  3000
后端 (Express):  8000
PostgreSQL:      5432
Nginx:           80, 443
```

**说明**：首期不使用 Redis，后期如需缓存功能再添加

### 5.3 后端 API 端点

#### 反馈相关
- `POST /api/feedback` - 提交反馈
  - Body: `{ name?, email, subject?, message }`
  - Response: `{ success: true, message: "Feedback submitted" }`

#### 健康检查
- `GET /api/health` - 服务健康状态

### 5.4 Cloudflare R2 配置

#### 环境变量（`.env`）
```env
# Database
DATABASE_URL="postgresql://user:password@localhost:5432/toolibox"

# Cloudflare R2
R2_ACCOUNT_ID=your_account_id
R2_ACCESS_KEY_ID=your_access_key
R2_SECRET_ACCESS_KEY=your_secret_key
R2_BUCKET_NAME=toolibox-feedback

# Application
PORT=8000
NODE_ENV=production
FRONTEND_URL=https://toolibox.com
```

#### R2 SDK 配置示例
```javascript
import { S3Client, PutObjectCommand } from "@aws-sdk/client-s3";

const r2Client = new S3Client({
  region: "auto",
  endpoint: `https://${process.env.R2_ACCOUNT_ID}.r2.cloudflarestorage.com`,
  credentials: {
    accessKeyId: process.env.R2_ACCESS_KEY_ID,
    secretAccessKey: process.env.R2_SECRET_ACCESS_KEY,
  },
});
```

### 5.5 数据文件结构示例

#### `/data/categories.json`
```json
[
  {
    "id": "text-tools",
    "slug": "text-tools",
    "icon": "📝",
    "name": {
      "en": "Text Tools",
      "zh": "文本工具"
    },
    "description": {
      "en": "Process and manipulate text easily",
      "zh": "轻松处理和转换文本"
    },
    "gradient": "linear-gradient(135deg, #667eea 0%, #764ba2 100%)",
    "order": 1
  }
  // ... 其他4个类目
]
```

#### `/data/tools.json`
```json
[
  {
    "id": "word-counter",
    "slug": "word-counter",
    "categoryId": "text-tools",
    "icon": "📊",
    "name": {
      "en": "Word Counter",
      "zh": "字数统计工具"
    },
    "description": {
      "en": "Count words, characters, and more",
      "zh": "统计字数、字符数等信息"
    },
    "comingSoon": true
  }
  // ... 其他29个工具
]
```

---

## 六、项目目录结构

```
toolibox/
├── frontend/                           # Next.js 前端项目
│   ├── src/
│   │   ├── app/
│   │   │   ├── [locale]/              # 国际化路由
│   │   │   │   ├── page.tsx           # 首页
│   │   │   │   ├── category/
│   │   │   │   │   └── [slug]/
│   │   │   │   │       └── page.tsx   # 类目页
│   │   │   │   ├── tools/
│   │   │   │   │   └── [slug]/
│   │   │   │   │       └── page.tsx   # 工具占位页
│   │   │   │   ├── about/
│   │   │   │   │   └── page.tsx
│   │   │   │   ├── privacy/
│   │   │   │   │   └── page.tsx
│   │   │   │   ├── terms/
│   │   │   │   │   └── page.tsx
│   │   │   │   └── feedback/
│   │   │   │       └── page.tsx
│   │   │   └── layout.tsx
│   │   ├── components/
│   │   │   ├── layout/
│   │   │   │   ├── Header.tsx
│   │   │   │   ├── Footer.tsx
│   │   │   │   └── Breadcrumb.tsx
│   │   │   ├── home/
│   │   │   │   ├── Hero.tsx
│   │   │   │   └── CategoryCard.tsx
│   │   │   ├── category/
│   │   │   │   └── ToolCard.tsx
│   │   │   └── ui/                    # shadcn/ui 组件
│   │   ├── data/
│   │   │   ├── categories.json        # 类目数据
│   │   │   └── tools.json             # 工具数据
│   │   ├── lib/
│   │   │   ├── utils.ts
│   │   │   └── api.ts                 # API 调用封装
│   │   └── locales/
│   │       ├── en.json                # 英文翻译
│   │       └── zh.json                # 中文翻译
│   ├── public/
│   │   ├── favicon.ico
│   │   └── images/
│   ├── package.json
│   ├── next.config.js
│   ├── tailwind.config.js
│   └── tsconfig.json
│
├── backend/                            # Node.js 后端项目
│   ├── src/
│   │   ├── routes/
│   │   │   ├── feedback.ts            # 反馈路由
│   │   │   └── health.ts              # 健康检查
│   │   ├── controllers/
│   │   │   └── feedbackController.ts  # 反馈控制器
│   │   ├── services/
│   │   │   ├── r2Service.ts           # R2 备份服务
│   │   │   └── backupCron.ts          # 定时备份任务
│   │   ├── middleware/
│   │   │   ├── cors.ts                # CORS 配置
│   │   │   └── errorHandler.ts        # 错误处理
│   │   ├── config/
│   │   │   └── database.ts            # 数据库配置
│   │   └── app.ts                     # Express 应用入口
│   ├── prisma/
│   │   ├── schema.prisma              # Prisma 模型
│   │   └── migrations/                # 数据库迁移
│   ├── package.json
│   ├── tsconfig.json
│   └── .env.example
│
├── nginx/
│   └── toolibox.conf                  # Nginx 配置文件
│
├── colors.css                         # 全局配色方案
├── .gitignore
└── README.md
```

---

## 七、开发计划

### Phase 1: 项目初始化（1-2天）
- [ ] 创建 Next.js + TypeScript 前端项目
- [ ] 创建 Node.js + Express 后端项目
- [ ] 配置 Tailwind CSS（导入 colors.css）
- [ ] 配置 next-intl 国际化
- [ ] 配置 Prisma + PostgreSQL
- [ ] 创建数据文件（categories.json、tools.json）
- [ ] 设置项目结构

### Phase 2: 布局组件（2-3天）
- [ ] 开发 Header 组件（Logo + 导航 + 语言下拉框）
- [ ] 开发 Footer 组件
- [ ] 开发 Breadcrumb 组件
- [ ] 配置响应式布局

### Phase 3: 首页开发（3-4天）
- [ ] 开发 Hero Section
- [ ] 开发 CategoryCard 组件（渐变卡片，基于 colors.css）
- [ ] 实现卡片悬停动画
- [ ] 响应式适配

### Phase 4: 内页开发（3-4天）
- [ ] 类目页（ToolCard 组件 + 网格布局）
- [ ] 工具占位页（Coming Soon 模板）
- [ ] About / Privacy / Terms 页面
- [ ] Feedback 页面（表单）

### Phase 5: 后端开发（3-4天）
- [ ] 数据库表设计（Prisma Schema）
- [ ] 反馈 API 接口（POST /api/feedback）
- [ ] Cloudflare R2 集成
- [ ] 定时备份任务（node-cron）
- [ ] 错误处理和日志

### Phase 6: 本地测试与VPS部署（2-3天）

#### 本地测试
- [ ] 本地测试（`npm run dev` 前后端联调）
- [ ] 功能验证（首页、类目页、工具页、反馈）
- [ ] 国际化测试（中英文切换）
- [ ] 响应式测试（桌面、平板、手机）

#### VPS环境准备
- [ ] 安装 Node.js 20
  ```bash
  curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
  sudo apt install -y nodejs
  ```
- [ ] 安装 PostgreSQL
  ```bash
  sudo apt install postgresql postgresql-contrib
  sudo systemctl start postgresql
  sudo systemctl enable postgresql

  # 创建数据库
  sudo -u postgres psql
  CREATE DATABASE toolibox;
  CREATE USER toolibox_user WITH PASSWORD 'your_password';
  GRANT ALL PRIVILEGES ON DATABASE toolibox TO toolibox_user;
  \q
  ```
- [ ] 安装 Nginx
  ```bash
  sudo apt install nginx
  sudo systemctl start nginx
  sudo systemctl enable nginx
  ```

#### 项目部署
- [ ] Clone 项目到 VPS
  ```bash
  cd /var/www
  git clone <your-repo> toolibox
  cd toolibox
  ```
- [ ] 配置环境变量（.env）
  ```bash
  cd backend
  cp .env.example .env
  nano .env
  # 填写数据库连接、R2配置等
  ```
- [ ] 前端构建
  ```bash
  cd frontend
  npm install
  npm run build
  ```
- [ ] 后端部署
  ```bash
  cd ../backend
  npm install
  npx prisma migrate deploy
  npm run build
  ```
- [ ] 使用 PM2 管理后端进程
  ```bash
  npm install -g pm2
  pm2 start dist/app.js --name toolibox-api
  pm2 save
  pm2 startup
  ```

#### Nginx配置
- [ ] 配置反向代理
  ```bash
  sudo nano /etc/nginx/sites-available/toolibox
  ```
  ```nginx
  server {
      listen 80;
      server_name toolibox.com www.toolibox.com;

      # 前端
      location / {
          proxy_pass http://localhost:3000;
          proxy_http_version 1.1;
          proxy_set_header Upgrade $http_upgrade;
          proxy_set_header Connection 'upgrade';
          proxy_set_header Host $host;
          proxy_cache_bypass $http_upgrade;
      }

      # 后端API
      location /api/ {
          proxy_pass http://localhost:8000;
          proxy_http_version 1.1;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      }
  }
  ```
  ```bash
  sudo ln -s /etc/nginx/sites-available/toolibox /etc/nginx/sites-enabled/
  sudo nginx -t
  sudo systemctl reload nginx
  ```

#### SSL证书配置
- [ ] 安装 Certbot
  ```bash
  sudo apt install certbot python3-certbot-nginx
  ```
- [ ] 申请 SSL 证书
  ```bash
  sudo certbot --nginx -d toolibox.com -d www.toolibox.com
  ```
- [ ] 测试自动续期
  ```bash
  sudo certbot renew --dry-run
  ```

#### Cloudflare R2配置
- [ ] 配置 R2 Bucket
- [ ] 测试备份功能
- [ ] 配置定时备份任务

#### 上线检查
- [ ] 域名解析正确
- [ ] HTTPS 正常工作
- [ ] 前后端正常访问
- [ ] 数据库连接正常
- [ ] R2 备份正常
- [ ] SEO优化（sitemap、meta标签）
- [ ] 性能测试（Lighthouse）

**总计**: 约 2-3 周

### 本地开发命令
```bash
# 前端开发
cd frontend
npm install
npm run dev          # 运行在 http://localhost:3000

# 后端开发
cd backend
npm install
npm run dev          # 运行在 http://localhost:8000

# 数据库迁移
cd backend
npx prisma migrate dev
npx prisma studio    # 打开数据库管理界面
```

---

## 八、SEO 优化

### 8.1 每个页面的 Meta 标签
```tsx
// 首页
<title>Free Online Tools | TooliBox</title>
<meta name="description" content="30+ free online tools for text processing, file conversion, image editing, and more. No sign-up required." />

// 类目页
<title>Text Tools - 6 Free Tools | TooliBox</title>
<meta name="description" content="Free text processing tools including word counter, case converter, and more." />

// 工具页
<title>Word Counter - Free Online Tool | TooliBox</title>
<meta name="description" content="Count words, characters, paragraphs in your text. Free and easy to use." />
```

### 8.2 Sitemap 生成
- 使用 Next.js 自动生成 `sitemap.xml`
- 包含所有页面（首页、5个类目页、30个工具页、说明页）

### 8.3 Open Graph 标签
每个页面包含 OG 标签，用于社交媒体分享

---

## 九、临时设置（已确认）

### ✅ 确认的配置

1. **Logo设计**：暂时使用文字 Logo "TooliBox"

2. **配色方案**：使用 `colors.css` 中的配色
   - 主色调：`#4A9C82`（清新绿）
   - 背景色：`#F5F7F2`（浅灰绿）
   - 强调色：`#FF9F66`（橙色）

3. **反馈系统**：自建后端 API + PostgreSQL + Cloudflare R2 备份

4. **反馈邮箱**：`feedback@toolibox.com`（临时）

5. **R2 Bucket 名称**：`toolibox-feedback`

6. **说明页文案**：由开发过程中提供标准模板

---

## 十、下一步行动

### ✅ 需求已确认：
- ✅ 首页 + 占位页方案
- ✅ 反馈系统方案（PostgreSQL + R2）
- ✅ 配色方案（colors.css）
- ✅ 30个工具占位列表
- ✅ 技术栈（Next.js + Node.js + PostgreSQL）

### ⏭️ 立即开始：
1. **创建前端项目**（Next.js + TypeScript + Tailwind）
2. **创建后端项目**（Express + Prisma + PostgreSQL）
3. **创建数据文件**（categories.json、tools.json）
4. **开发语言切换下拉框组件**
5. **开发首页**（Hero + 5个渐变卡片）

### 本地开发流程：
```bash
# 1. 前端
cd frontend && npm install && npm run dev

# 2. 后端
cd backend && npm install && npm run dev

# 3. 浏览器访问
http://localhost:3000
```

---

**文档状态**: ✅ 已确认，可以开始开发
**最后更新**: 2025-12-08

**核心确认**:
- ✅ 技术栈：Next.js + Express + PostgreSQL + R2
- ✅ 配色方案：基于 colors.css
- ✅ 反馈系统：完整的后端 API + 数据库 + R2 备份
- ✅ 30个工具占位（不实现具体功能）
- ✅ 语言切换：下拉框形式（便于扩展更多语言）
- ✅ 部署方式：**不使用 Docker**，本地用 `npm run dev` 测试
